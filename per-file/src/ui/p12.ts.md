# src/ui/p12.ts

| Metric | Coverage | |
| --- | --- | --- |
| Statements | 98.67% (74/75) | `██████████` |
| Branches | 75.00% (6/8) | `████████░░` |
| Functions | 100.00% (17/17) | `██████████` |
| Lines | 98.59% (70/71) | `██████████` |

**Uncovered lines:** 150

**Partial branches:** L139 (if), L159 (binary-expr)

_Legend: `-` uncovered statement · `!` branch with an untaken path · `⋮` covered lines skipped · gutter: line number._

```diff
  136 |   if (trimmed.includes("-----BEGIN PRIVATE KEY-----")) {
  137 |     return pemToDer(trimmed);
  138 |   }
! 139 |   if (trimmed.includes("-----BEGIN EC PRIVATE KEY-----")) {
  140 |     const ecPrivateKey = new pkijs.ECPrivateKey({ schema: asn1js.fromBER(pemToDer(trimmed)).result });
  141 |     const privateKeyInfo = new pkijs.PrivateKeyInfo({
  142 |       privateKeyAlgorithm: new pkijs.AlgorithmIdentifier({
  ⋮
  147 |     });
  148 |     return privateKeyInfo.toSchema().toBER(false);
  149 |   }
- 150 |   throw new Error("Unsupported private key format. Expected PEM 'PRIVATE KEY' or 'EC PRIVATE KEY'.");
  151 | }
  152 | 
  153 | export async function buildP12(
  ⋮
  156 |   password: string,
  157 |   friendlyName: string,
  158 | ): Promise<Buffer> {
! 159 |   const name = friendlyName || "pontes-user";
  160 |   const certDer = toForgeBytes(pemToDer(certPem));
  161 |   const privateKeyInfoDer = toForgeBytes(normalizePrivateKeyToPkcs8Der(keyPem));
  162 | 
```
